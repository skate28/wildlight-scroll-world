{
  "name": "wildlight-scroll-world",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test test/booking-utils.test.js",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "xrpl": "^5.0.0",
    "xrpl-connect": "^0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
